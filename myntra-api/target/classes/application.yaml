
server:
  servlet:
    port: 8080
    contextPath: /api

spring:

  application:
    name: myntra-api

  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/myntradb
    username: postgres
    password: Nitish@2000

  jpa:
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
